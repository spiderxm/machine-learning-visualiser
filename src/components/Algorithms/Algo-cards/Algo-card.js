import React from "react";
import {Card} from "semantic-ui-react";

const card = (props) => {
    //Card will
    // contain logo
    // name of the algorithm
    // description of algorithm

    // card will be sleeping of long screens and standing for small screens
    return (
        <React.Fragment>
            <div>Mrigank anand is a good boy</div>
        </React.Fragment>
    );
}

export default card